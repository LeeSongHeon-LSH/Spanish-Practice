[Unit]
Description=LSHobby DB 백업 + 복원 리허설 (pg_dump, 주 1회) — 타이머

[Timer]
OnCalendar=Sun *-*-* 01:00:00
# PC가 꺼져 있어 놓친 회차는 켜지면 바로 돈다 (cron엔 없던 것)
Persistent=true
Unit=lshobby-backup.service

[Install]
WantedBy=timers.target
