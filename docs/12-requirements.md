> LSHobby 설계 문서 — 목차·로드맵·§번호↔파일 매핑은 [README](README.md) 참조

> **개정 (2026-09-02, 코드 대조)**: 홈 타임라인(FR-02·NFR-01)은 화면에 없고 서랍 3개 + 개수뿐. FR-11 하루 신규 한도·FR-14 집중 모드 폐기(#81), FR-12 cloze는 30% 확률, FR-06 검색 위치, SEC-04·NFR-02·§12.6-4는 Storage 미사용이라 조건부. 생각 세션 FR-50번대 신설. FR-40 아이콘 결손 기록.
> **개정 (2026-08-20, 결정 #57~61 반영 완료)**: CS 세션 제거 · 인용구 삭제가 코드(커밋 6246582~)와 DB(마이그레이션 20260820090000 · 20260820100000)에 모두 반영됐다. 본문은 현행 상태로 개정됨 — CS/quote 관련 폐기 항목은 사료 표시.

## 12. 요구사항 명세 (SRS) — 확정 (2026-08-14)

> 여기서 SRS = **Software Requirements Specification**. §6의 SRS(Spaced Repetition System)와는 무관한 동음 약어.

§2~§11에 흩어진 요구사항을 검증 가능한 형태로 한곳에 모으고, 설계 단계에서 비어 있던 **보안(§12.4)·백업(NFR-04) 요구를 신규 확정**한다. 구현 중 "이거 하기로 했었나?"는 이 문서로 판정한다.

### 12.1 범위와 우선순위

- **[C]** = 컷오버 필수 (1차 출시: 스페인어 모듈 전체 + 홈·activity·reflection 골격, §6.5)
- **[P]** = 컷오버 이후 (책 §7, CS §8, 영어 확장 §6.2)

### 12.2 기능 요구사항 (FR)

**공통 (shared)**

| ID | 우선 | 요구 | 근거 |
|---|---|---|---|
| FR-01 | C | Supabase Auth 이메일 로그인. 회원가입 UI 없음 (계정은 대시보드에서 생성) | §11.2 |
| FR-02 | C | 홈 허브 = 서랍 3개(책·언어·생각) + 도메인별 보유량(각 모듈의 count 공개 API). ~~도메인 횡단 타임라인~~ **#53으로 화면 제거** — `activity_feed`는 적재만 하고 앱 화면에서는 읽지 않는다(`getFeed`는 미소비, 소비자는 Notion 백업·다이제스트 배치 §16.11·16.12) | §5.2·5.3 |
| FR-03 | C | activity 발행 규칙 — 언어: 단어 건별 + 학습 일별 1건 / 책: 전부 건별. 원본 삭제 후에도 이벤트 보존 (~~CS 규칙~~ #57 폐기) | §6.4·7.2·9.3 |
| FR-04 | C | reflection: 엔티티당 스레드 1개(다형), append-only entry(content + context). 수정·삭제 UI 없음 — 정정도 새 entry로 | §4 |
| FR-05 | P | 자유 태그: book 대상 다형 태깅 (~~concept~~ #57 폐기) | §7.1 |
| FR-06 | P | 텍스트 검색은 **목록별**(단어장 · 생각 메모 내용/주제)로 한정 — 전역 통합 검색은 미설계 (§12.5, #58로 서재 검색은 목차가 대체, 독서 기록 책 선택은 여정 순 목록) | §7.3 |

**언어 (컷오버 대상)**

| ID | 우선 | 요구 |
|---|---|---|
| FR-10 | C | 단어 CRUD (word·gender·meaning), `norm` 중복 차단 — 악센트 관대·ñ 엄격 (§6.2) |
| FR-11 | C | FSRS 스케줄링(`ts-fsrs`) — 정답=Good/오답=Again 자동 매핑. ~~새 카드 일일 20개 한도~~ **폐기 (2026-08-31, #81)** — 출제 순서는 `session.practiceOrder`(§6.3, due 복습·신규 1:1 교대 #82), 끝은 사용자가 [종료] |
| FR-12 | C | 퀴즈 3방향(스↔한 타이핑 50:50, **30% 확률로 예문 cloze 시도** — 예문·빈칸 없으면 타이핑 폴백) (#41 개정) |
| FR-13 | C | Tatoeba 예문 조회·저장, 단어당 fetch 기록 (§9.2) |
| FR-14 | C | ~~어려운 단어 집중 모드~~ **폐기 (2026-08-31, #81)** — 정답률 오름차순 정렬(`practiceOrder`)에 흡수 |
| FR-15 | C | 통계·스트릭 — 전부 `es_review_log` 파생 집계, 카운터 컬럼 없음 (#36) |
| FR-16 | C | CSV export (§11) |
| FR-17 | C | 시드 218단어 이식 (§6.5) |
| FR-18 | P | 영어 확장 = `en_*` 테이블 복제 + 언어 config 등록만으로 완결되어야 함 (§6.2) |

**책 (컷오버 이후 — CS 항목은 #57로 폐기)**

| ID | 우선 | 요구 |
|---|---|---|
| FR-20 | P | 책 CRUD — 텍스트 5필드 수기 입력, 표지 없음 (§7.1) |
| FR-21 | P | 회독 기록(완독일 + 선택 별점), 재독 = 새 레코드 (§7.1) |
| FR-22 | P | ~~인용구~~ **폐기 (2026-08-20, #58)** — quote 테이블 drop |
| FR-23 | P | 책당 마크다운 노트 1개, reflection entry `context`에 "N회독" 자동 기입 (§7.2) |
| FR-24 | P | 책 삭제 시 회독·노트 + 다형 행(reflection·tagging)까지 앱 레이어에서 일괄 삭제 (§9.3) |
| FR-30~34 | P | ~~CS 개념·위키링크·이미지~~ **폐기 (2026-08-20, #57)** — concept 계열 테이블·Storage 버킷 drop |
| FR-40 | C | PWA 설치형 최소 — manifest + 기본 캐싱, 오프라인 퀴즈 없음 (#19). 아이콘은 `scripts/generate-icons.mjs`가 마스코트 도트에서 생성(#60). **2026-09-02 현재 `public/icons/icon-512.png`가 작업 트리에서 삭제돼 manifest 참조가 깨진 상태** — 스크립트 재실행으로 복구 |

**생각 (`/thoughts`, 2026-08-21~)**

| ID | 우선 | 요구 |
|---|---|---|
| FR-50 | P | 생각 기록 append-only — 내용 + 주제, 수정·삭제 UI 없음(§4 원칙, `thought` 테이블) |
| FR-51 | P | 검색 — 내용·주제, 최근 30일 주제 칩. 날짜 그룹 스트림 + 더 보기 페이징 |
| FR-52 | P | 하루 요약(`thought_digest`)은 **앱이 읽기만** — 채우는 쪽은 집 PC 로컬 LLM 배치(§16.11), 날짜 축 KST(#78) |
| FR-53 | P | activity 발행 = 건별(`thought`/`created`). 생각 데이터는 외부 API·Notion 백업으로 나가지 않는다(§16.11·16.12) |

### 12.3 비기능 요구사항 (NFR)

| ID | 요구 |
|---|---|
| NFR-01 | **성능**: 홈 = 도메인별 count 3회 병렬 조회(~~타임라인 쿼리~~ 없음, #53), 퀴즈 채점 = 클라이언트 즉시. 수치 목표는 두지 않음(1인 사용) — 체감 병목 발생 시 대응 |
| NFR-02 | **모바일**: 터치 타겟 44px, 하단 탭, 입력 폼 2~3필드 (§2.3). ~~이미지 클라이언트 리사이즈~~ — 이미지 기능이 CS와 함께 폐기(#57)돼 사용처 없음, 첨부 도입 시 부활 |
| NFR-03 | **가용성**: best-effort. Supabase 무료 프로젝트는 장기 무활동 시 일시정지될 수 있고(정지 시 대시보드에서 재개), **2026-08-30부터는 집 PC가 꺼져 있으면 앱도 안 열린다**(§16.5 — 1인 사용이라 수용) |
| NFR-04 | **백업**: 무료 티어는 자동 백업이 없다고 전제 — `pg_dump` **주 1회** 로컬 보관 + Storage 파일 포함, 복원 리허설 최소 1회로 절차 검증 (§2.2 체크리스트의 구체화) → **2026-09-06 자동화**(§16.14): `lshobby-backup.timer` 일요일 주 1회 pg_dump + **매회 복원 리허설**(행 수 대조), 실패 시 알림 |
| NFR-05 | **이식성**: §2.2 락인 회피 체크리스트 준수 (호스팅 전용 저장소 금지, 환경변수, 표준 Next.js). 2026-08-30 Vercel → 집 PC 이전으로 실증 |
| NFR-06 | **유지보수성**: 모듈 경계 규칙(§3.4) 준수, vitest 단위 테스트(FSRS 엔진·채점·norm 규칙), e2e 초기 생략 (§6.5) |
| NFR-07 | **용량**: 무료 한도(DB 500MB · Storage 1GB · 대역폭 100GB/월) 내 운영 — 텍스트 위주 + 이미지 리사이즈로 충분 |

### 12.4 보안 요구사항 (SEC) — 신규 확정

위협 모델: **테일넷 전용 URL + 사용자 1인** (2026-08-30 개정 — 그 전에는 공개 URL). 실질 위협은 ① 낯선 사람의 계정 생성·접근 ② 키 유출 ③ 저장 콘텐츠 XSS 세 가지가 전부다. 앱이 인터넷에서 사라지면서 ①의 표면은 더 좁아졌지만, **RLS와 SEC-01이 여전히 최종 방어선**이다 — Supabase는 계속 인터넷에 열려 있기 때문이다(§16.3).

| ID | 우선 | 요구 |
|---|---|---|
| SEC-01 | C | **신규 가입 서버 차단** — Supabase Auth 설정에서 "Allow new users to sign up" **off**, 본인 계정은 대시보드에서 수동 생성. 회원가입 UI가 없어도 Auth API로 직접 가입이 가능하므로 UI 부재만으로는 불충분. RLS가 "authenticated 전부 허용"(#33)이라 **이 설정이 사실상 유일한 접근 통제선** |
| SEC-02 | C | RLS 전 테이블 활성 + **anon 전부 거부** (§9.2 그대로). 2026-08-18 #51로 뒀던 `cv_document` anon SELECT 예외는 **2026-08-30 #73으로 해소** — CV가 별도 리포로 나가며 테이블째 drop, 예외 없는 원안으로 복귀 |
| SEC-03 | C | 키 관리 — `service_role` 키는 서버 환경변수 전용, 클라이언트 번들·리포 커밋 금지. 브라우저에는 anon key만. `.env*`는 gitignore |
| SEC-04 | C | **조건부 — 현재 Storage 미사용**(CS 폐기 #57로 버킷·정책 drop, 마이그레이션 20260820090000). 버킷을 다시 만들 때: **private**(공개 버킷 금지) + authenticated 정책, 이미지 표시는 signed URL 또는 인증 경유 |
| SEC-05 | P | 마크다운 렌더 시 HTML sanitize(rehype-sanitize 등) — 입력자가 본인뿐이어도 저장 XSS를 습관적으로 차단, 비용 0 |
| SEC-06 | C | HTTPS 전제(2026-08-30부터는 `tailscale serve`가 테일넷 인증서로 종단 — §16.5). CSP 튜닝은 여전히 스코프 외 — 단 **무비용 최소 헤더 3종은 추가** (2026-08-18 #56 → 2026-08-27 #72 개정): `X-Content-Type-Options: nosniff` · `Referrer-Policy: strict-origin-when-cross-origin` · `X-Frame-Options: DENY`(클릭재킹 차단, #72) (next.config.ts `headers()`) |
| SEC-07 | — | **명시적 비채택**: 레이트리밋·감사 로그·2FA·세션 정책 커스텀 — SEC-01이 성립하면 1인 규모에서 실익 없음 |
| SEC-08 | C | **비밀번호 분실 복구 = 운영 절차** (아래 런북). 앱 내 "비밀번호 찾기" 플로우는 비채택 — 재설정 페이지·이메일 설정이 필요해지는데, 프로젝트 소유자 본인이라 대시보드/admin API로 즉시 재설정 가능하므로 코드 0줄이 이김. 다중 사용자 확장 시에만 앱 내 플로우 추가 |

> SEC-08 보완 (2026-08-15, #50): **기억하고 있을 때의 변경**은 홈 ⚙ → 비밀번호 변경(로그인 세션 필요). 아래 런북은 **잊었을 때의 복구** 전용.

**SEC-08 복구 런북** — 앱 비밀번호를 잊었을 때:

1. Supabase 대시보드에 로그인한다 (앱 비밀번호와 무관한 별도 인증). **이 대시보드 계정이 진짜 최상위 복구 수단**이므로 이쪽 로그인 수단(GitHub 계정 등)을 잃지 않는 것이 중요
2. Authentication → Users에서 유저의 UUID 확인. 대시보드가 비밀번호 직접 변경 UI를 제공하면 그걸로 끝
3. 아니면 admin API 한 줄 (service_role 키는 서버 환경변수에 이미 보유, SEC-03):

```bash
curl -X PUT "https://<project-ref>.supabase.co/auth/v1/admin/users/<user-uuid>" \
  -H "apikey: $SERVICE_ROLE_KEY" -H "Authorization: Bearer $SERVICE_ROLE_KEY" \
  -H "Content-Type: application/json" -d '{"password":"새 비밀번호"}'
```

이메일 발송이 없으므로 SMTP 설정·재설정 페이지가 전혀 필요 없다. 세팅 단계(§12.6)에서 이 절차를 1회 실제로 리허설해 검증한다.

### 12.5 공백 점검 결과 (기능·비기능 완성도 확인)

이번 점검에서 확인된 미비점과 처리:

- **가입 차단 미명시** → SEC-01로 확정 (설계 문서 전체에서 유일한 실제 보안 구멍이었음)
- **백업 주기·절차 미정** → NFR-04로 확정 (§2.2에 "정기 백업" 한 줄뿐이었음)
- **`shared/search` 모듈(§3.2) 미설계** → 전역 검색은 스코프에서 제외하고 목록별 검색(FR-06)으로 한정. 필요해지면 후속 설계
- **reflection entry 수정 정책 미정** → append-only 원칙대로 수정·삭제 UI 없음 확정 (FR-04)
- **모니터링·로깅** → 별도 도구 비채택 (2026-08-30부터는 `journalctl --user -u lshobby`)
- 그 외 기능 요구는 §5~§11에서 전부 확정되어 있음을 확인 — 화면 인벤토리(§11) 기준 누락 없음

### 12.6 수용 기준

컷오버 판정 = §6.5 기능 기준 + 본 문서 **[C] 전 항목** 충족.

특히 SEC-01~04는 **Supabase 프로젝트 생성 직후** 체크리스트로 즉시 적용한다:

1. Auth signup off 확인
2. anon key로 REST 직접 호출 시 전 테이블 거부 확인 (RLS 동작 검증)
3. `service_role` 키가 클라이언트 번들·리포에 없음 확인
4. ~~Storage 버킷 private 확인~~ — 버킷 없음(SEC-04 조건부). 버킷을 만드는 날 다시 켠다
5. 비밀번호 재설정 런북(SEC-08) 1회 리허설 — admin API로 실제 변경해 보고 새 비밀번호로 로그인 확인
