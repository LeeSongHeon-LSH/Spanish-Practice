> LSHobby 설계 문서 — 목차·로드맵·§번호↔파일 매핑은 [README](README.md) 참조

## 10. 결정 사항 요약

> **기록 규약** (2026-09-02부터): 한 항목은 *맥락(무엇이 문제였나) → 검토한 대안 → 택한 것과 이유 → 되돌아볼 신호*를 담는다.
> 개정은 새 번호로 쓰고 원 항목 번호를 가리킨다("#81 개정") — 원 항목은 지우지 않는다. 왜 그렇게 골랐는지를 나중에 다시 읽으려는 기록이므로,
> 결과만이 아니라 **틀렸던 가정**(예: #82의 "복습은 유한하다")도 남긴다. 커밋 해시는 코드 diff로 가는 손잡이.

| # | 항목 | 결정 |
|---|---|---|
| 1 | 아키텍처 | **Modular Monolith** (MSA 아님) |
| 2 | 기술 스택 | Next.js + Tailwind + Supabase + Vercel |
| 3 | 비용 | 무료 티어 (AWS 미사용) |
| 4 | 반응형 | 모바일 우선, PWA 검토 |
| 5 | Reflection 방식 | **A안 (append-only)** |
| 6 | Reflection 위치 | **shared 모듈 핵심 기능** (세 도메인 공통) |
| 7 | Reflection 참조 | **다형 참조** (앱 레이어에서 무결성 보장) |
| 8 | 화면 구조 | **B안** — 통합 홈(허브) + 세션별 전용 공간 |
| 9 | 홈 구성 | 세션 선택 카드 + 최근 기록 타임라인 |
| 10 | 타임라인 구현 | `activity_feed` 이벤트 테이블 (summary 비정규화) |
| 11 | 기존 스페인어 코드 | 기능·데이터를 `language` 모듈로 흡수 (코드는 재작성, §6.5) |
| 12 | 앱 이름 | **LSHobby** (리포 `LeeSongHeon-LSH/LSHobby`) |
| 13 | 전환 방식 | **빅뱅 재작성** — 컷오버 기준 = 스페인어 기능 패리티 (§6.5) |
| 14 | 언어 구조 | **B안** — 언어별 테이블 + 코드 공용 엔진 (§6.2) |
| 15 | SRS | **FSRS**(`ts-fsrs`) — 정답=Good / 오답=Again 자동 매핑 |
| 16 | 홈 재검토 | 복습 배지 없음 — §5.2 원안 유지 |
| 17 | activity 발행 | 단어 추가 건별 + 학습 일별 요약 1건 |
| 18 | 디자인 | azulejo 폐기, 전면 재디자인 |
| 19 | PWA | 설치형 최소, 오프라인 퀴즈 없음 |
| 20 | 책 메타데이터 | 텍스트 5필드(제목/저자/옮긴이/출판사/원저연도), 표지 없음, 수기 입력 (§7.1) |
| 21 | 책 등록 범위 | 완독한 책만 — 위시·독서 상태 없음 |
| 22 | 장르 | 자유 태그 (`shared/tag` 재사용) |
| 23 | 회독 | 완독 이벤트만(완독일 + 선택 별점), 재독 = 새 레코드 |
| 24 | 인용구·정리 | `quote` 테이블(본문·페이지·코멘트) + 책당 마크다운 노트 1개 |
| 25 | 책 기록 시점 | 완독 후 일괄 기록, 중간 기록 없음 (§7.2) |
| 26 | 개념 단위 | 주제 단위 마크다운 문서 — 제목·본문·태그만, 요약·상태 필드 없음 (§8.1) |
| 27 | 개념 연결 | 태그 + 본문 위키링크(`concept_link` 백링크), 계층 트리 없음 (§8.2) |
| 28 | 위키링크 규칙 | 제목 매칭·제목 변경 시 일괄 치환, dangling 링크 허용(red link) |
| 29 | 코드 스니펫 | 별도 테이블 없음 — 마크다운 코드펜스 |
| 30 | CS 스코프 | SRS 복습 없음·벌크 임포트 없음, 이미지 첨부는 지원(Storage) |
| 31 | CS activity | 생성·reflection 건별 + 본문 수정은 개념당 당일 1건 갱신 |
| 32 | PK 타입 | 전 테이블 `bigint` identity — uuid 기각 (§9.3) |
| 33 | RLS | 켜되 "authenticated 전부 허용", `user_id` 컬럼 없음 (1계정 전제) |
| 34 | 태그 스키마 | 다형 조인 — `tag` + `tagging(subject_type, subject_id)` |
| 35 | 다형 참조 값 | 테이블 특정 값(`'es_word'` 등) — §4.4 초안의 `'vocab'` 정정 |
| 36 | 학습 카운터 | 컬럼 없음 — `es_review_log`에서 파생 집계 (이중 저장 제거) |
| 37 | ERD/DDL | §9 확정 — cascade 이원화(FK=DB/다형=앱), activity 이벤트 영구 보존 |
| 38 | 와이어프레임 | 텍스트(ASCII) 형식, 전 세션 화면 한 번에 (§11) |
| 39 | 내비 문법 | 홈은 탭바 없음, 세션 탭 첫 슬롯 = [홈] 허브 복귀 — 세션 간 이동은 홈 경유 (§11.1) |
| 40 | 세션 탭 | 언어 5탭(현행 이식) / 책 [홈·서재·인용구] / CS [홈·개념] |
| 41 | 퀴즈 형식 | 현행 3방향(스↔한 타이핑, 예문 있으면 cloze 우선) 그대로 이식 |
| 42 | 요구사항 명세 | §12 확정 — FR/NFR/SEC 통합, 컷오버 판정 = §6.5 + §12 [C] 전 항목 |
| 43 | 보안 | **Supabase 가입 서버 차단이 핵심 통제선** (UI 부재만으론 불충분), service_role 서버 전용, Storage private, 마크다운 sanitize (§12.4) |
| 44 | 백업 | 무료 티어 자동 백업 없음 전제 — `pg_dump` 주 1회 + 복원 리허설 1회 (§12 NFR-04) |
| 45 | 명시적 비채택 | 레이트리밋·감사로그·2FA·전역 통합 검색(목록별 검색으로 한정) — 1인 규모 실익 없음 (§12.4·12.5) |
| 46 | 비밀번호 복구 | 앱 내 "비밀번호 찾기" 없음 — 대시보드/admin API 운영 절차로 재설정 (§12 SEC-08 런북, 세팅 시 리허설 1회) |
| 47 | 유지보수 문서 | 유저 스토리(§13)·흐름도(§14)·DCD(§15) — 다이어그램은 mermaid, 규칙 원본은 §12(충돌 시 §12 우선) |
| 48 | 재디자인 시점 | **UI 전면 재디자인은 컷오버 조건에서 제외** — 전 기능(책·CS 포함) 구현 후 1회 실시 (2026-08-15, §6.5 원안 수정). 퀴즈의 listen·gender 방향도 이식 제외 확정. **2026-08-18 실시 완료 (#55)** |
| 49 | 컷오버 실행 | **2026-08-15 컷오버 완료** — 파이썬 앱·systemd 3유닛·pytest CI 삭제, spanish.db는 홈 디렉터리 백업만 유지 |
| 50 | 비밀번호 변경 | 홈 ⚙ 메뉴에 **로그인 상태 변경 UI** 추가(`auth.updateUser`) — 세션 필요라 공격면 추가 없음. 분실 복구는 여전히 SEC-08 런북(앱 내 "찾기" 없음 유지) |
| 51 | CV 공개 페이지 | **루트 `/` = 공개 CV**(로그인 세션은 `/home`으로 이동 — 홈 이사) + `/cv` 상시 경로. 신규 `cv` 모듈 + `cv_document` 1행 마크다운, **첫 anon SELECT RLS**(쓰기는 authenticated — SEC-02 개정). 서버 렌더(매 요청)·색인 허용·이메일 그대로 노출·한국어 단일 (§17) |
| 52 | CV 편집·로그인 진입 | 편집 `/cv/edit`은 CS 편집 패턴 재사용, 진입점은 홈 4번째 카드(📄 CV). 초기 콘텐츠 빈 상태, activity 미발행. 로그인 진입은 CV 상단 **도트 고양이 SVG 클릭**(이스터에그) — 텍스트 링크 없음 (§17.6) |
| 53 | #48 범위 확정 | 재디자인 때 홈을 **4등분 버튼(CV 수정/책/CS/언어)으로 대체**(타임라인 제거), 모바일도 2×2, 하단 탭 문법 유지. CV 페이지 비주얼 다듬기 포함 (§17.8) |
| 54 | 영어 확장 | `enConfig`(대소문자·앞뒤 공백 무시 정확 일치, 관대 비교·성별·특수문자 보조 없음) + `en_*` 4테이블(§6.2 — gender 컬럼 제외). 예문 원문 컬럼 일반화 `es_text`→`text`. **언어 전환 = 학습 랜딩 헤더 ▾ + localStorage 전역**(§11.4.1) — 세션 5화면 공용. 홈 카드 단어 수는 전 언어 합계 |
| 55 | 디자인 시스템 | #48 실행 — **"네 서랍"**: 종이(#f5f4ef)+먹(#211f1a) 위 도메인 4색 인덱스(책 초록·언어 파란펜·CS 앰버·CV 보라=고양이 모자색). 타이포 = 고운바탕(제목)/IBM Plex Sans KR(본문)/IBM Plex Mono(수치·상태). 홈 = 2×2 서랍(#53 구조) + 16×16 픽셀 아이콘 세트(§17.6 고양이 문법 확장). 탭바 도메인색 인디케이터, 그림자 없는 1px 괘선 카드 |
| 56 | 최소 보안 헤더 | 공개 CV로 anon 표면이 생긴 것을 계기로 SEC-06 개정 — `nosniff`·`Referrer-Policy` 2종만 next.config.ts로 추가 (CSP·기타 헤더는 여전히 비채택, #45 유지) |
| 57 | CS 세션 제거 | **CS(knowledge) 모듈 폐기** (2026-08-20, 목업 확정) — 홈 = 책/언어/CV **3서랍**(데스크톱 3×1·모바일 1×3, #53 4등분 개정). §8 전체·CS 화면(§11.6)·concept 계열 테이블 폐기 예정. **2026-08-20 코드·DB 반영 완료** — §9 DDL 개정, 마이그레이션 20260820090000(concept 계열)·20260820100000(quote) |
| 58 | 책 세션 재구성 | **독서 여정 책장 = 서재** — 한 보(步) = 완독 20권 = 책등 하나, 펼치면 목차 + 권당 속표지 1쪽(데스크톱 양면·모바일 한 쪽 넘김). **인용구 기능 삭제**(#24 폐기 — 기록은 회독별 감상만, 인용 블록 없음). 구 "책 상세" 화면 폐지 → 여정 **"자세히보기" 시트**(회독 이력 + 감상 + 재독 추가)로 대체. 완독 기록 플로우(§7.2)는 유지 |
| 59 | 내비 문법 개정 | #39·#40 폐기 — **탭바의 [홈] 슬롯 제거, 홈 복귀 = 우상단 버튼**(도트 서랍 아이콘, 도메인색 손잡이). 언어 = 4탭 [학습·단어장·통계·추가], **책 세션 = 탭바 없음**(여정 책장이 단일 진입점). 비주얼 원본 = 목업 아티팩트 「LSHobby 네 서랍 목업」 |
| 60 | 펭귄 리테마 | #55 "네 서랍" 개정 — 마스코트 **마법사 고양이 → 도트 펭귄 가족**: 잠옷 펭귄(CV·로그인 이스터에그 승계), 책 든 펭귄(책), 말풍선 펭귄(언어). 배경 눈밭(#eef1f4)·먹(#22262b), 도메인 재대응 **책=빙하 파랑 #4d7fa3 / 언어=부리 주황 #d9821f / CV=잠옷 로즈 #c05e7c**. 이모지 아이콘 전폐 → 16×16 도트·스트로크 SVG(홈 버튼 = 도트 서랍장). 홈 서랍마다 도메인 펭귄 배치 |
| 61 | CV 비주얼 확정 | §17.8 잔여 항목 실행 — **"이력서 = 종이 한 장"**: 눈밭 위 종이 시트(#fffdf7·1px 괘선), 시트 위 모서리에 잠옷 펭귄(클릭=로그인, §17.6 유지 필수). **섹션 필터 칩** 신설: 마크다운 h2 기준 클라이언트 토글 — 무선택=전체 보기, 같은 칩 재탭=전체 복귀. 서버는 전체 렌더(색인 불변)·스키마 변경 없음. 내용이 길면 페이지 스크롤(기본 동작) |
| 62 | 성능 개편 | 보안·성능 리뷰(2026-08-20) 반영 — ① **리뷰 집계를 DB RPC로**(`*_word_stats`·`*_daily_stats` 4종, 마이그레이션 20260820120000): 클라이언트 전량 조회는 PostgREST 1000행 캡에서 조용히 틀려지는 구조였음(#36 파생 원칙은 유지, 집계 위치만 이동) ② **공개 CV 정적화 + 저장 시 revalidatePath**(#51 "매 요청 렌더" 개정 — 즉시 반영은 유지, `/api/revalidate-cv`) ③ CV 마크다운 RSC 렌더·redirect 경량화(공개 페이지 JS ~44KB gz 절감) ④ AuthGuard 낙관적 렌더, 여정 넘김 animationend·모션 축소 시 즉시 점프, setTags 배치 upsert, 시트 병렬 조회 + 단건 태그(tagsOf) ⑤ 보안 리뷰는 취약점 0건(XSS 2중 차단·RLS 표면·API 검증 확인) |
| 63 | 철학 정보 탭 철회 | **철학 정보 탭(SEP Llama3 문답 + exaone 한↔영 통역) 당일 도입·철회** (2026-08-24) — 8B 모델 수준이 낮아 세부 논변을 신뢰하기 어렵고, 휘발성 채팅이라 reflection 핵심 가치에 기여 없음. tailnet 노출(tailscale serve :8443·OLLAMA_HOST 바인딩·CORS·Vercel `NEXT_PUBLIC_OLLAMA_URL`)까지 전부 원복, Ollama는 localhost 기본 복귀. 다이제스트 배치는 무관하게 유지 (§16.11). 코드·구성 기록은 git 히스토리 보존 |
| 64 | Notion 활동 미러 | **activity_feed → Notion DB 단방향 미러** (2026-08-24, grill 세션) — 집 PC cron 00:40 배치(`scripts/sync-activity-notion.mjs`), 커서 저장소 없이 Notion의 최대 activity_id에서 재개, 어제까지만 전송(당일 upsertDaily 미확정 보호). **thought 도메인 제외**(§16.11 반출 금지 유지, 포함 목록 강제). 어댑터 계층 비채택 — 행→표준 이벤트→전송 함수 경계만 분리, 두 번째 대상 생길 때 추상화 (§16.12) |
| 65 | 남극 장면 | #60·#61 확장 — **CV = 남극 지평선**: 화면 하단 고정 배경에 빙하 산(24×14 도트, 빙하 톤 #cfdfe9·#a9c4d6 신설)·픽셀 눈드리프트 지면·꼬마 펭귄·발자국, 성근 눈내림(고정 배치 13개, reduced-motion 시 숨김). 종이 시트(z-1)가 장면(z-0) 위에 얹히는 구조, 잠옷 펭귄 클릭=로그인(§17.6) 유지 + hover 뒤뚱. **홈은 절제**: 서랍 hover/포커스 시 도메인 펭귄 뒤뚱, 서랍장 아래 발자국 트레일→꼬마 펭귄. 장면 소품은 도메인 3색 미사용(빙하 톤만), 다른 세션 화면은 불변 |
| 66 | 세션별 장면 | #65 확장 — 네 세션 대표 화면에 도트 장면(공통: `-z-10` 하단 고정 레이어, 콘텐츠는 항상 위). **홈** = CV와 동일한 IceScene(#65 발자국 스트립은 중복이라 철회). **책 = 이글루 서재**: 얼음 블록 벽 타일(48×24)·설원 보이는 둥근 창·나무 책장·앉아 책 읽는 펭귄. **생각 = 백야의 밤**: 지평선 띠만 남보라(#2b2850) 밤하늘 — 잔별 반짝임·초승달·도트 오로라 리본(블러 CSS 시안은 탁해서 기각), 하늘 올려다보는 펭귄 전용 스프라이트. **언어 = 수다 빙하**: 말풍선 "안녕!/Hello!/¡Hola!"(실 학습 언어 en·es + 한국어), 모바일은 탭바 위에 지면. 작업 화면(단어장·통계·퀴즈 등)은 불변, 모든 모션 reduced-motion 시 정지 |
| 67 | 전면 하늘 | #66 확장 — 장면 배경을 지면 띠에서 **뷰포트 전체 하늘**로: 홈·CV·로그인·CV 편집 = 얼음빛(`sky-ice`), 언어 = 오후 햇살(`sky-chat`, 해·구름 소품), 생각 = 해질녘 라벤더→밤 그라데이션(`sky-night`, 초저녁 별), 책 = 이글루 돔 전체 얼음 블록(`igloo-sky`). 장면을 레이아웃으로 이동해 **작업 화면(단어장·통계·추가·퀴즈·완독 기록)까지 커버**, 로그인·CV 편집에도 IceScene 추가 — 콘텐츠 카드가 불투명(bg-card)이라 가독성 유지. **펭귄 증원**(장면당 7~9): 아기 펭귄·배밀이 펭귄 스프라이트 신설, 이글루엔 읽어주는 펭귄+듣는 아기 스토리타임 구도 |
| 68 | 책장 벽·전면 밤 | #67 개정 — **서재**: 얼음 벽 띠·낱개 책장 → **하단 20vh 책장 벽**(나무 선반+도메인색 책등 48×28 타일을 셀당 2px로 확대 렌더, 눕혀 쌓인 책 더미로 반복 완화), 읽는 펭귄들은 벽 앞으로. **생각**: 지평선 밤 띠 → **화면 전체 밤하늘 그라데이션**(짙은 남보라→지평선 라벤더), 잔별·초승달·오로라를 상공으로 — 어두운 배경이라 카드 밖 텍스트는 전용 토큰 `night-ink`/`night-faint`로 반전. 고아가 된 PixelBookshelf 스프라이트 제거(git 히스토리 보존) |
| 69 | 장면 미리보기 서랍 | #60 "홈 서랍" 개정 — 홈의 네 서랍 카드 배경을 단색 소프트 톤에서 **각 세션 장면의 핵심부**로: 책 = 이글루 타일+책장 벽 띠, 언어 = 오후 하늘+해+발자국, CV = 얼음 하늘+빙하 산, 생각 = 밤하늘+잔별+초승달 (전부 기존 장면 클래스·스프라이트 재사용, 새 에셋 없음). 아이콘·이름·개수는 **흰 2px 테두리 + 기존 소프트색 판**으로 감싸 장면 위 가독성 보장. 도메인 탭 띠·hover 뒤뚱·등장 애니메이션 유지. **이어서 CV 서랍 제거**(#73으로 CV가 리포 밖으로 나간 뒤 외부 링크만 남아 있던 것) — 홈은 **책·언어·생각 3서랍**, 모바일 1×3 · 데스크톱 3×1로 재배치 |
| 70 | 장면 리뷰 보수 | #67~#69 코드 리뷰(xhigh) 반영 — ① **밤하늘 대비**: `.sky-night`가 지평선에서 #7d75b2까지 밝아져 night-faint가 1.7:1까지 떨어졌음 → 가장 밝은 정지점을 **#565080으로 제한**하고 토큰을 #d3cdee/#f5f3ff로 상향(모든 정지점 AA). **하늘을 다시 밝히려면 토큰도 함께 올려야 한다** ② 먹색 포커스 링이 밤하늘에서 1.04:1 → 하늘 위 컨트롤에만 `.focus-night` ③ **책장 벽 치수 단일화**: `--shelf-h`/`--shelf-lip`/`--shelf-tile`을 :root에 정의해 CSS와 서재 스크롤 영역이 같은 값 사용(vh/dvh 불일치·6px 턱 누락 해소), 홈 서랍 미리보기는 변수만 재정의 ④ 어른+아기 펭귄을 flex 묶음으로 — %위치와 px크기를 섞어 360px에서 서로를 뚫고 그려지던 문제 ⑤ 이글루 바닥 그리기 순서, span>div 중첩, 색 리터럴 중복(→ `--color-night-dot`·`.igloo-floor` 재사용). **테스트 `src/design.test.ts` 신설**(77→149): 도트 격자 불변식·모션 축소 커버리지·밤하늘 대비를 소스에서 검사, 변형 주입으로 검출력 확인 |
| 71 | 리뷰 보수 2차 | #70 코드·보안 리뷰 반영 — **#70의 테스트가 헛통과하고 있었음**: ① 모션 축소 검사가 주석을 안 걷어내고 `toContain`이라 `.aurora`를 주석 처리해도, `display:none`을 `color:red`로 바꿔도 통과 → 주석 제거 후 **실제로 모션을 끄는 규칙의 셀렉터만** 인정 ② 대비 검사가 6자리 hex만 읽어 정지점을 `rgb()`로 쓰면 무통과 → 다른 표기가 섞이면 먼저 실패시키고 정지점 개수 가드를 같은 it으로 ③ 스프라이트 개수 항진 명제 → 내보낸 `Pixel*` 전수 대조. **네 변형 모두 실패함을 확인**. 그 밖에: 포커스 링 누락 2곳(HomeButton·검색 입력, `.focus-night`에 자손 선택자 추가), **서재 스크롤 격리를 책장 화면에만** 적용(기록 폼은 문서 스크롤 — 키보드가 오르면 폼이 212px로 눌렸음), 장면 4개 `memo`+홈 서랍 배열 모듈 스코프(입력 한 글자마다 수백 rect 재조정 제거), `--tab-h`·`--color-wood-dark` 토큰화, **`--shelf-tile`은 48의 정수배여야 함**(1.29배는 도트가 들쭉), 이글루 벽 타일을 책장과 같은 2배 밀도로, keyframe의 dvh→vh(주소창 접힘 시 튐), 서랍 개수 텍스트 2.5:1→5.3:1, `/thoughts` 레이아웃으로 PWA 상태바 색 분리. **보안 리뷰는 취약점 0건**(임시 우회했던 AuthGuard 5곳 전부 복원 확인) |
| 72 | 클릭재킹 차단 | #56 개정 — 보안 점검(2026-08-27)에서 남은 유일한 실질 벡터가 **공개 CV의 iframe 삽입**(타 사이트가 광고·피싱 페이지에 통째로 끼워넣기)으로 확인 → `X-Frame-Options: DENY` 헤더 1종 추가. 삽입을 허용할 시나리오가 없어 DENY 고정. CSP(`frame-ancestors` 포함)는 여전히 비채택(#45 유지) — 작성자 1인 + rehype-sanitize(SEC-05) 구조에서 튜닝 비용 대비 실익 없음 |
| 73 | CV 분리·로컬 호스팅 전환 | **공개면과 사적 공간을 리포 단위로 가름** (2026-08-30). ① **CV → 별도 리포 `LeeSongHeon-LSH.github.io`** (GitHub Pages): 원본은 `cv.md` 한 장, `next build --output export`로 굳혀 배포 — `cv_document` 테이블 drop(마이그레이션 20260830080000)으로 **전 스키마 유일했던 anon SELECT 예외가 사라져 SEC-02가 원안으로 복귀**. 편집 화면(`/cv/edit`)·재검증 API 폐기, 홈 CV 서랍은 GitHub `cv.md` 편집으로 나가는 외부 링크 ② **루트(`/`) = 취미공간의 문**: 중앙에 큰 마스코트 하나, 누르면 `/login` — §17.6 이스터에그를 공개 CV 상단에서 여기로 이관(원칙 유지), 공개 문서가 아니게 됐으므로 `robots: noindex` ③ **Vercel → 집 PC 로컬 호스팅**: `systemd --user` + `loginctl enable-linger`로 `next start :3000` 상시 구동, 외부 접근은 **`tailscale serve` (테일넷 전용, funnel 아님)**. GitHub 연동 해제로 푸시 자동배포 종료 — 배포는 `scripts/deploy-local.sh`를 도는 **2분 주기 systemd 타이머**가 origin/main을 대조해 받아서 빌드·재시작 — 더티 트리·분기·빌드 실패는 전부 "돌던 사이트 유지"로 처리. 443이 아니라 **8443**인 것은 이 PC의 kind 클러스터가 이미 0.0.0.0:443을 점유해서다 (§16.5) |
| 74 | 배포 무중단화·리뷰 보수 3차 | #73 자동 배포 코드 리뷰(xhigh) 반영 — ① **"빌드 실패해도 직전 빌드가 서빙된다"가 거짓이었다**: `next build`는 `cleanDistDir` 기본값 때문에 컴파일 전에 `.next`를 비우므로, 실패한 순간 재시작을 안 해도 사이트가 통째로 깨지고 (`$old == $new`가 되어) 타이머가 재시도조차 안 했다 → **`NEXT_DIST_DIR=.next-staging`에 짓고 빌드 성공 + 새 `BUILD_ID`의 200 응답까지 확인한 뒤 `mv`로 교체**, 헬스체크 실패 시 직전 `.next`로 롤백 ② **CI 게이트**: `.githooks/pre-push`는 `--no-verify`·GitHub UI 머지를 못 막았다 → `gh api .../check-runs`로 그 sha의 CI가 초록일 때만 배포(`DEPLOY_SKIP_CI=1`이 탈출구) ③ **가드 보강**: 이 체크아웃의 `npm run dev` 감지, 로컬이 origin보다 앞선 경우와 진짜 갈라진 경우 분리, 갈라짐·CI 정체는 `notify-send` + 타이머 정지로 **사람을 부른다** (같은 실패를 2분마다 720번 찍던 침묵 해소) ④ **고아 정리**(#69가 남긴 것): `DRAWERS` 명시 타입 주석·`ReactNode` import, `--color-cv`/`--color-cv-soft` 토큰, `digest-thoughts.mjs`의 `cv` 도메인 라벨, 죽은 주석 4곳 ⑤ 홈 서랍 격자를 **개수 비의존**으로(`grid-rows-3`/`md:grid-cols-3` → `auto-rows-fr`/`md:auto-cols-fr`) — 배열만 고치면 배치가 따라온다 ⑥ **여정 넘김 탭존이 스크롤을 먹던 문제**: #58로 지면(LEAF)이 스크롤러가 됐는데 좌우 64px 탭존 버튼은 그 형제라 스크롤 체인 밖이었다 → 지면 자신의 가장자리 클릭으로 처리(스크롤한 탭은 click을 내지 않는다), 키보드·스크린리더용 넘김은 모서리 버튼이 유지 ⑦ **문서 정합**: 홈 서랍 수(README·docs/05·11·17), 테이블 수 19/16 → **실측 17** (docs/09·16) ⑧ **정보 위생**: public 리포의 문서가 테일넷 실주소를 그대로 싣고 있었다 → `<호스트>.ts.net:8443` 자리표시자로. 구성 근거(유닛 이름·`journalctl`·8443을 쓰는 이유)는 문서 가치라 그대로 두고 주소만 뺐다 |
| 75 | 펼친 책이 벽 앞으로 | #68 개정 — 목차 지면이 쪽 안에서 스크롤되고 있었다(#58이 넣은 안전망이 상시 동작). 원인은 `max-h` 캡이 아니라 **레이아웃이 경로만 보고 책장 규칙을 여정 뷰에 씌운 것**: `/library`는 책장과 펼친 책 두 상태를 같은 라우트에서 그리는데 `library/layout.tsx`가 둘 다 `100dvh − 벽` 안에 가뒀다(869px 화면에서 책에 남는 높이 ~520px, 목차 10줄은 ~616px 필요). **높이 규칙을 화면이 지게** 옮기고 — 책장은 여전히 벽 위에서만 스크롤(#68 유지), 펼친 책은 뷰포트 전체를 쓰되 새 토큰 `--shelf-figures`(72px)로 **벽 앞에서 읽는 펭귄 띠만 비운다**. 책은 벽을 42%쯤 덮고 아래로 책등이 계속 보인다 — `perspective: 1600px`가 이미 선언해 둔 깊이를 레이아웃이 막고 있던 모순을 푼 것. 겹침이 충돌이 아니라 깊이로 읽히도록 그림자를 접촉 그림자로 (`0 10px 30px/.08` → `0 18px 44px −12px/.38`) — 기존 값은 옅은 얼음 하늘 위에서만 성립해 나무 벽(#77603f) 위에서는 사라져 책이 벽에 붙어 보인다. 넘김 오버레이도 같은 값으로 맞춰 애니메이션 중 그림자가 튀지 않게. 쪽 상한 560 → 680px(지면 비례 1:1.6 유지). 넘김 힌트는 이제 벽 위에 앉으므로 `text-faint`가 안 읽혀 **책과 같은 종이 조각**에 얹었다(테두리 없음 — 책 가장자리와 경쟁). **쪽을 키우는 것만으로는 안 되는 이유가 따로 있었다** — 목차 10줄이 고정 높이(`py-2.5`+글줄=40.5px)로 쌓여 **필요 높이가 화면과 무관하게 524px로 먼저 정해진다**. 지면이 그보다 짧아지는 순간 넘치므로, 상한을 올리는 것은 특정 뷰포트만 맞추는 대증요법이다 (크로미움 실측: 지면 안쪽 498px에서 +26, 438px에서 +86, 378px에서 +146. 1366×768 노트북은 브라우저 UI를 빼면 지면이 ~465px이라 여기 걸린다). → 줄을 `flex-1`로 바꿔 **지면에 남은 높이를 10등분해 나눠 갖게** 했다(`min-h-9`~`max-h-13`; 바깥 버튼이 세로 가운데 정렬을 맡고 번호·제목·점선·쪽번호의 밑선 정렬은 안쪽에 span 하나를 더 두어 그대로 유지). 높이를 화면에서 받아 오므로 뷰포트가 달라져도 넘치지 않고, 10줄이 36px 밑으로 내려가는 아주 짧은 화면에서만 #58의 스크롤 안전망이 실제로 동작한다 |
| 76 | 퀴즈 입력 보수 | 2026-08-31~09-02 네 커밋(814e9e1·290a4eb·e0bf25d·b85b6da). **Enter 한 번에 채점 화면이 건너뛰어지던 세 경로**를 헤드리스 Chrome으로 전수 확인해 막았다: ① keydown이 phase를 바꾸고 "다음"이 autoFocus를 받은 뒤 같은 키의 keypress가 그 버튼을 누른다(브라우저는 버튼 Enter를 keypress에서 클릭으로 바꾼다) → 입력창 Enter 분기에 preventDefault ② 오토리핏 — 누르고 있으면 반복 keydown이 버튼 클릭으로 이어져 한 번에 6문제가 지나갔다 → `!e.repeat` ③ next() 재진입 — await 사이에 두 번째 호출이 끼어 문제 하나가 출제 없이 소모됐다 → advancing ref. 그 위에 **canAdvance ref: 채점 화면이 실제로 그려진 뒤(이펙트 시점)에만 "다음"을 받는다** — 사용자가 다시 누르는 동작만 진행으로 인정. 발음은 방향 조건을 걷어 세 유형 모두 채점 시 한 번, 앞 발음은 cancel(큐 지연으로 엉뚱한 단어가 들리던 것). 빈칸은 indexOf → 유니코드 단어 경계(`clozeIndex`, "Solo quiero sol"의 sol을 Solo 안에서 잡던 것). **답안 저장 실패는 삼키지 않고 누적 수로 헤더·종료 화면에 표시** — 문제별 플래그는 늦게 온 실패가 다음 문제에 붙어 어긋나서 누적으로. **회귀 테스트는 없다**: 브라우저 기본 동작이라 jsdom으로 재현되지 않는다 — 대신 확인한 경로 목록을 커밋 본문에 남겼다 |
| 77 | 배포 트리거 기준 | #74 개정 (2026-08-31, 3ff0bab). `HEAD != origin/main`으로 배포를 판정하면 **이 PC(개발기=서버, §16.7)에서 직접 커밋·푸시할 때 둘이 같이 올라가 "받을 것 없음"이 된다** — 814e9e1이 배포 안 된 채 남아 있었고 로그엔 Finished만 찍혔다. PR 머지 흐름에서만 우연히 동작하던 것. 기준을 **"지금 서빙 중인 빌드가 어느 커밋인가"(`.next/DEPLOYED_SHA`)**로: 빌드 성공 시 스테이징에 쓰고 교체와 함께 옮기므로 롤백 시 기록도 따라간다. package-lock 비교도 같은 커밋. 함께: eslint 무시 범위를 .gitignore와 일치(`.next-staging`·`.next-prev`가 남으면 lint 412개 에러로 push가 막히던 것) |
| 78 | 날짜 축 = KST 고정 | 2026-08-31 (ae6e928). digest-thoughts.mjs가 "앱의 dayKey와 같은 축"이라며 **로컬 타임존**으로 날짜를 잘랐는데, 브라우저는 KST·서버는 UTC(Etc/UTC)라 같은 축이 아니었다 — KST 00~09시 메모가 전날 요약에 묶였다(실제 1건 어긋남). **배치의 날짜 축은 전부 KST 고정**(sync-notion-backup.mjs의 kstDate와 통일), todayStart도 "오늘 00:00 KST". 원칙: 서버 로컬 시간을 믿지 않는다 — 앱이 쓰는 축을 코드로 박는다. 기존 행은 LLM 재실행 없이 day만 옮겼다 |
| 79 | 뜻 동의어 반영 | 2026-08-31 (ecdc9aa·e9888a8). 채점기는 콤마 구분 뜻을 전부 정답으로 받는데 저장된 뜻이 하나뿐이라 "소년"에 "남자아이"가 오답이었다. **Gemini 제안 → 사람 검수 → 반영의 2단계 배치**(`backfill-meanings.mjs`, 제안 JSON + `--apply`): 잘못된 동의어 하나가 그 단어를 영영 헐겁게 채점하므로 LLM 출력을 곧장 DB에 넣지 않는다. 재실행 안전은 스키마가 아니라 제안 파일이 진다(동의어 없음 판정도 파일에 남겨 재질문 방지). 코드 필터: 콤마 포함·20자 초과·기존 뜻 중복·단어당 3개 초과. **검수 결과 160건 중 14건 제외** — 두 부류: 같은 덱의 다른 단어 뜻과 겹치는 것(distinguish/식별하다↔identify 등, 기계로 6건)과 의미 범위가 어긋난 것(saber/인지하다, paradox/모순 등, 사람만 거른다). 경계선(difícil/힘든)·사어(셈틀)는 남김. **제시문은 앞의 두 뜻까지만**(`promptMeaning`) — 뜻이 길어지면 한→대상 제시문이 힌트 덩어리가 된다. 채점·정답 공개는 전체 유지, 자르는 규칙은 grading과 같은 함수에 두어 표시와 채점이 갈라지지 않게 |
| 80 | Notion 백업 DB 통합 | 2026-08-31 (e9c8ad1). 08-27부터 5일간 00:40 배치가 404로 조용히 실패해 백업이 0건. 통합이 볼 수 있는 DB는 최상위 "LSHobby" **하나뿐**이고 책·단어 속성과 행이 이미 그 안에 섞여 있었다(두 DB가 합쳐진 상태). **DB를 다시 나누지 않고**(기록 보관 성격) 스크립트를 그 구조에 맞춤 — 이미 있던 "유형" select("책 여정"/"단어 대시보드")로 읽기·생성을 가른다. 유형은 생성 경로에만 넣는다: bookProps에 넣으면 sync_hash가 전부 바뀌어 불필요한 재동기화. 환경변수는 `NOTION_BACKUP_DB_ID` 하나로 — 구 `NOTION_DB_ID`를 재사용하지 않은 것은 §16에 "구 활동 DB"로 기록된 다른 변수라서. 교훈: **배치 실패가 5일간 안 보였다** — 알림 없는 cron은 실패해도 모른다 (§16.12 운영 항목) |
| 81 | 퀴즈 한 모드 | 2026-08-31 (5825a4b·9fdc32c). 모드가 셋이었다 — due(오늘 할당)/free(∞)/hard. free는 정답을 풀에서 빼지 않아 끝이 없는데도 답마다 answerWord가 돌아 **아직 시작도 안 한 새 단어를 재스케줄하고 신규 일일 한도를 깎았다** — 연습이 SRS 일정을 덮어쓰고 있었다. **셋을 하나로**, 끝은 사용자가 종료 버튼으로. **하루 할당(신규 20개 한도, #15 시절 관례) 폐지** — 구 앱 이식물이었고 "오늘 몫"이라는 개념이 무제한 연습과 충돌. 어려운 단어 모드는 정답률 오름차순에 흡수. 가중 추첨(pickWeight)은 결정적 정렬로 대체. **한 바퀴 = 전체 단어**라 한 세션에서 같은 단어를 두 번 안 만나고, 그래서 FSRS가 한 답에 한 번만 반영된다(10분 안에 여러 번 재스케줄돼 간격이 무의미해지던 문제가 구조적으로 사라짐). FSRS는 남는다 — due가 "지금 복습할 유한한 집합"의 경계. 랜딩 카드의 "복습 대기 N / 연습 ∞"도 제거 — 무제한 연습에 due 개수를 약속하면 어긋나 보인다. 출제 순서는 이때 "복습 전부 → 신규"였고 #82에서 개정 |
| 82 | 출제 순서 = 복습·신규 섞어 내기 | #81 개정 (2026-09-02). #81의 "복습 전부 → 신규 → 아직 due 아닌 것"은 *복습은 유한, 신규는 무한이니 유한한 쪽을 먼저 비운다*는 논리였으나 **실사용에서 신규를 한 번도 못 만났다**. 실측(es): 신규 172 / 본 것 46 / 그중 지금 due 25, Learning 상태 24 — 오답을 Again으로 넣으면 FSRS가 1분·10분 뒤 due로 잡아 **틀린 단어는 다음 세션 시작 시 거의 항상 due**이고, 구간 안이 정답률 오름차순이라 가장 많이 틀린 단어가 매번 맨 앞. 세션이 due 수보다 짧으면 신규 0. 즉 "복습이 굶는다"를 막으려다 신규가 굶었다. 검토한 대안: ⓐ 그대로(25문제 넘게 풀면 신규 나옴) ⓑ **복습·신규 교대(택)** ⓒ 세션 시작에 신규 k개 선출제. ⓑ는 양쪽 다 굶지 않고 Anki 기본 동작과 같으며 k 같은 상수가 없다. 비율은 1:1 — 정답률이 떨어지더라도 새 단어를 만나는 쪽이 학습에 낫다는 판단(사용자). 한쪽이 바닥나면 남은 쪽을 잇고, 그 뒤 아직 due 아닌 것. 한 바퀴 규칙·FSRS 1회 반영은 그대로. 되돌아볼 신호: 정답률이 오래 낮게 머물면 복습 2:신규 1로 (interleave 호출부 한 곳) |
| 83 | 문서 전수 대조 | 2026-09-02. §2~§17 열여섯 문서를 코드·마이그레이션·스크립트와 전수 대조해 약 100건을 고쳤다(생각 세션 누락, 하루 할당·집중 모드·타임라인 잔재, DCD 함수명, 배포 트리거·Notion 구조 등). **문서에서 발견했지만 코드는 고치지 않은 것** — 문서에 "알려진 예외"로 적고 코드 수정은 따로 판단한다: ① `language/words.ts`의 deleteWord가 shared 소유 `reflection_thread`를 직접 지운다(§3.4·§15.6 위반 — `removeThread` 공개 API가 있음) ② `api/sentence` 라우트가 `language/tatoeba` 내부 파일을 직접 import(공개 인터페이스 경유 규칙 위반) ③ `activity_feed`는 앱 화면 소비처가 없다 — 쓰기 전용(#53 이후), 소비자는 Notion 백업·다이제스트 배치뿐 ④ `manifest.ts`가 가리키는 `icon-512.png`가 작업 트리에서 삭제된 상태 ⑤ 개발 클론 `.env`에 구 Notion 변수 3종·Vercel 토큰·미사용 Supabase 키가 남아 있고 `NOTION_BACKUP_DB_ID`가 없다(서버 쪽 .env는 별도) ⑥ `shared/search`는 빈 스텁. 교훈: **커밋 메시지에만 남은 결정이 일곱 건(#76~#82)이었다** — 결정 로그를 커밋과 같은 날 쓰지 않으면 문서가 코드에서 멀어진다 |
| 84 | 생각 세션 = 달력 + 하루 시트 | 2026-09-06. 스트림이 위에서부터 날짜 그룹을 전부 펼쳐 쌓아서, 생각이 몇 달만 쌓여도 스크롤이 끝없이 늘어났다(입력창·검색·주제 칩·되짚기 카드까지 전부 세로로 붙어 있었다). **검토한 대안 셋**: ① *날짜별 접기* — 실제로 구현까지 갔다. 행 수는 줄지만 **여전히 아래로 자란다**(1년이면 365행), 접힌 줄의 "하루 요약 첫 줄" 색인은 좋았다. ② *토스트* — 사용자 제안. 기각: 생각은 길이가 제각각(짧은 메모~긴 정리)이고 하루에 여러 개면 스크롤이 필요한데 **자동으로 사라지는 창은 읽는 속도를 화면이 정한다**. 주제 칩으로 검색까지 넘어가야 하니 사라지는 표면 위에서 조작하는 셈. ③ **달력 + 시트 (택함)** — 한 달은 늘 6줄이라 **쌓여도 세로가 자라지 않고**, 넘기는 횟수만 는다. 하루 요약이 이미 KST 하루 단위(#78)라 **칸 하나 = 요약 하나**로 데이터 축과 화면 축이 일치한다. 빈칸도 정보가 된다(며칠 비웠나·언제 몰아 썼나). 쓴 날 표시는 `PixelPenguinTiny` — 밤하늘 달력에 앉은 펭귄은 이 앱만의 언어고, 기록 직후 **오늘 칸에 펭귄이 앉는 것이 저장 확인**(별도 토스트 불필요). 시트는 책 세션 회독 기록 시트와 같은 문법(바텀시트, md~ 중앙)이라 새로 배울 게 없다. 되짚기도 카드 3묶음을 펼치던 것을 한 줄 + 같은 시트로. `listThoughts(limit, before)`·`recentDigests(limit)` → `thoughtsInMonth`·`digestsInMonth`로 교체(더 보기 페이징 폐지). **잃은 것**: 접힌 줄의 요약 색인 — 최근 며칠을 훑으려면 탭이 한 번 더 든다. **되돌아볼 신호**: ① 달력을 열자마자 매번 오늘을 눌러 읽는다면 색인이 필요하다는 뜻(칸에 요약 첫 줄을 못 넣으니 시트 대신 아래 패널로) ② 하루 3~4개를 넘어 칸 하나가 "너무 많은 하루"가 되면 펭귄 옆 개수 표기 재논의(지금은 일부러 뺐다 — 개수는 시트 제목에 있다) ③ 석 달 이상 거슬러 가는 일이 잦으면 연 단위 보기 |
| 85 | 달력은 흰 카드 위에 | #84 개정 (2026-09-06). #84는 "밤하늘 달력에 앉은 펭귄"을 근거로 달력을 하늘 위에 그대로 놓았는데, **가정이 틀렸다**: `PixelPenguinTiny`는 몸통 `#22262b`·배 `#fafbfc`로 **밝은 배경을 전제로 그린 스프라이트**라 밤하늘(#201c3e~#565080) 위에서는 몸통이 하늘에 묻히고 흰 배만 남아 흐릿한 얼룩으로 보인다 — 이 화면의 주인공이 가장 안 보이는 상태였다. 세 안(하늘 위 그대로 / 흰 카드 / 반투명 밤 패널)을 헤드리스 Chrome으로 나란히 렌더해 비교. 반투명 패널은 구조만 잡아줄 뿐 펭귄 가독성은 그대로였다. **흰 카드**로 감싸니 몸통·부리가 살아나고, 위의 입력창·검색창과 같은 문법(도메인 테두리 + 흰 배경)이라 페이지가 세로로 정돈된다. 날짜 글자는 `night-ink/night-faint`(하늘용) → `ink/faint`(흰 배경용)로 교체 — 안 바꾸면 흰 바탕에 연보라 글자가 된다. 하늘은 헤더·주제 칩·되짚기 행·시트 배경에 그대로 남는다. 강조 막대(`bg-thought` 틱)는 입력창에만 두고 달력엔 넣지 않았다 — 달력의 정체성은 펭귄이지 막대가 아니다. 함께: 입력창의 "쓴 생각은 고치지 않아요 — 이어서 쓰기" 삭제(기능 설명 문구는 UI에 달지 않는다는 규칙, append-only는 수정 버튼이 없다는 사실로 이미 드러난다). **되돌아볼 신호**: 흰 카드가 셋(입력·검색·달력)이라 하늘이 여백으로만 남는다면, 달력 대신 입력창을 접는 쪽을 검토 |
| 86 | 배치를 systemd 타이머로 + 실패 알림 + DB 백업 자동화 + CI 타입 검사 | 2026-09-06. "비어 있어서 위험한 것" 셋을 한 번에. **맥락**: ① NFR-04(pg_dump 주 1회)가 확정된 지 한 달 넘게 자동화 없음 — 생각 데이터는 Notion 미러에서도 빠지므로 **Supabase 한 곳에만 있는 대체 불가 데이터**였다 ② cron 배치는 #80처럼 닷새를 조용히 실패해도 모른다 ③ CI가 lint·test만 돌려 타입 오류가 CI를 통과하고 집 PC 빌드에서 터진다(롤백 설계로 서비스는 안 죽지만 배포가 조용히 멈춘다). **검토한 대안**: 백업은 (a) apt로 postgresql-client 설치 → sudo 필요·버전 불일치 가능 (b) Supabase 대시보드 수동 다운로드 → 무료 티어 없음 (c) **Docker의 Supabase Postgres 17 이미지로 pg_dump (택함)** — 서버와 같은 메이저, 이미 `supabase start`가 받아 둔 것, sudo 불필요. 알림은 (a) cron 줄에 `|| notify-send` → cron엔 세션 버스가 없어 알림이 안 뜨는 일이 잦다 (b) 별도 헬스체크 서비스(healthchecks.io 등) → 외부 의존 (c) **systemd 사용자 타이머 + `OnFailure=` 템플릿 유닛 (택함)** — 배포 타이머가 이미 같은 방식으로 `notify-send`를 쓰고 있어 알림이 실제로 뜬다는 게 검증된 경로. `Persistent=true`(놓친 회차 보충)와 journald 로그가 덤으로 온다. **복원 리허설을 매회**로 정한 이유: "최소 1회"는 그 뒤 스키마가 바뀌면 다시 거짓이 된다 — 9초짜리라 매주 돌리는 비용이 없다. 행 수 대조는 `n_live_tup` 추정치가 아니라 표마다 `count(*)`(906행이라 즉시). **틀렸던 가정 하나**: 첫 리허설에서 "pg_restore 오류 2건"이 찍혔는데 실은 `CREATE SCHEMA public` 중복 하나를 grep이 두 번 센 것 — 리허설 DB의 기본 public을 먼저 지우고 `^pg_restore: error`만 세도록 고쳐 0건. 다이제스트는 "전부 건너뜀"이 종료 코드 0이라 Ollama가 죽어도 조용했다 → 전부 실패면 1로. **되돌아볼 신호**: ① `~/.lshobby/alerts.log`에 같은 유닛이 사흘 연속 → 재시도 정책이 아니라 근본 원인 ② 덤프가 수십 MB로 커지면 리허설 시간 재측정 ③ `supabase start` 이미지를 지우면 `DUMP_IMAGE`가 사라진다 — `docker pull postgres:17`로 대체 가능(pg_dump는 표준) |
| 87 | DB 인터페이스 = Supabase 생성 타입 | 2026-09-06. **맥락**: 행 타입 11개(`Thought`·`Book`·`Word`…)를 손으로 쓰고 조회 결과를 `data as X[]`로 단언(13곳)하고 있었다 — 마이그레이션이 컬럼을 바꿔도 컴파일러가 모르고, 틀린 자리는 런타임 `undefined`로만 드러난다. **택한 것**: `supabase gen types`(CLI는 npx로, DB 직접 접속 `--db-url` — 풀러 세션 모드 + `~/.lshobby/db-password`, backup-db.sh와 같은 경로)로 `src/modules/shared/db/database.types.ts`를 만들고 클라이언트를 `SupabaseClient<Database>`로. 행 타입은 전부 `Tables<"표">` 파생으로 바꾸고 캐스트를 걷어냈다. **언어 모듈의 예외 하나**: `Word`의 `gender`는 DB엔 text, 앱엔 `"m"|"f"|"n"|"none"` 유니온이라 `Omit<Tables<"es_words">,"gender"> & {gender?: Gender}`로 좁히고 조회 결과 단언(`as Word`)은 그 좁힘 자리에만 남긴다 — 컬럼이 늘거나 줄면 `Word`가 따라 바뀌어 사용처·테스트의 `makeWord`가 깨진다(드리프트 검출은 유지). 표·RPC 이름은 `LanguageConfig`에서 `string` → 리터럴 유니온(`"es_words" \| "en_words"`)으로 — 언어 추가 시 유니온에 한 줄 붙인다. **부수 발견**: 조건부 스프레드 `...(hasGender ? {gender} : {})`는 `gender?: Gender \| undefined`가 되어 en_words 삽입 타입이 거부한다 → 두 갈래 리터럴로. **드리프트 테스트** `src/schema.test.ts`: 마이그레이션의 `create/drop table` 집합 == 생성 타입의 표 집합. 표 추가·삭제 후 재생성을 잊으면 CI가 잡는다. 컬럼 변경은 재생성으로만 잡힌다 — 그래서 §16.13에 "마이그레이션 뒤 `npm run db:types`" 규칙. **검토한 대안**: (a) 손 인터페이스 유지 + 컴파일 시점 호환 단언(`Tables<"x"> extends X`) — 절반의 안전 (b) `--linked`/`--project-id` — `supabase link`·액세스 토큰이 더 필요, `--db-url`이 이미 있는 비밀 하나로 끝난다. **되돌아볼 신호**: 언어가 셋 이상이면 리터럴 유니온을 `keyof Database["public"]["Tables"]` 기반 템플릿 타입으로 |
